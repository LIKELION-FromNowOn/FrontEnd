# 리뷰 회신 — 2026-08-21

**보내는 사람** 황인서 (프론트)
**받는 사람** 송원석 · 김민정 · 이철희
**기준** 리뷰해 주신 `406ab9a` → **지금 `f85b667`** (그 뒤로 5커밋 더 올라갔습니다)

> **지적 셋 다 처리했습니다.** 닉네임 변경은 붙였고, 첫 발자국 상세와 낡은 주석도 정리했습니다.
> **다만 `.env.example` 은 비어 있지 않습니다.** 3장에 적었습니다 — 다른 파일을 보신 것 같습니다.
> **회원가입 흐름은 리뷰 이후에 한 번 더 바뀌었습니다.** 4장을 먼저 봐주십시오.
> **그리고 아직 답을 못 받은 것이 여섯 건 있습니다.** 6장에 모아 뒀습니다.

---

## 1. 닉네임 변경 — 붙였습니다

원석님 결정대로 「준비 중」을 걷어내고 `PATCH /me` 에 연결했습니다.

- 게스트는 입력칸을 잠그고 버튼을 **「회원가입하고 바꾸기」** 로 바꿔 `/auth/email` 로 보냅니다
- 403 은 오류가 아니라 안내로 적었습니다 — 「회원가입을 하면 닉네임을 바꿀 수 있어요」
- 저장 성공 뒤 `forgetMe()` 를 부릅니다. 상단바가 같이 바뀝니다
- `ComingSoonBanner` 는 걷어냈습니다

**주신 코드에서 한 군데만 다르게 했습니다.**

```js
// 주신 것
useEffect(() => { if (me?.name) setName(me.name) }, [me])

// 바꾼 것
const [typed, setTyped] = useState(null)
const name = typed ?? me?.name ?? ''
```

이름을 상태에 복사해 두고 `useEffect` 로 맞추면, **서버 응답이 늦게 올 때 사용자가 이미 고치고 있던 글자를 덮어씁니다.** 손대기 전에는 서버 값을 그대로 쓰고, 한 글자라도 치면 그때부터 친 값을 쓰도록 했습니다. (린트도 `react-hooks/set-state-in-effect` 로 막습니다.)

**게스트 경로는 화면에서 확인했습니다.** 회원 경로는 실계정이 없어 확인 못 했습니다 — 7장에 같이 적었습니다.

---

## 2. 첫 발자국 상세 — 고쳤고, 한 발 더 갔습니다

지적하신 「항상 같은 카드」는 `eb6b807` 에서 고쳤습니다. 넘어갈 때 카드 id 를 실어 보내고, 상세는 그 id 로 `GET /footsteps` 8건에서 찾습니다. 홈·목록 둘 다 고쳤습니다.

**그런데 같이 알려주신 `NOW-STEP-005` 때문에 한 가지 더 바꿨습니다.**

> 「완료 버튼이 없다. 기록에 쌓지 않는다」

처음에는 「루틴 따라하기」를 **준비 중**으로 돌려놨는데, 그것도 틀렸습니다. **나중에 생길 기능이 아니라 원래 없는 것**이라, 준비 중이라고 하면 안 올 것을 온다고 말하는 셈입니다. 시작·완료 버튼 자체를 빼고 **「닫기」** 로 바꿨습니다(`2524165`).

따라가기를 저장하는 API 가 없던 것도 같은 이유였습니다. **앞서 「API 를 요청할까요」라고 여쭌 건 제가 잘못 짚은 것입니다** — 만들 기능이 아니었습니다.

홈 배치도 확인했습니다. 「오늘의 케어 하나만」 → 「오늘의 첫 발자국」 순서로, 명세의 「할 일이 먼저고 사례는 참고다」와 맞습니다.

---

## 3. 낡은 주석 — 정리했습니다. 다만 `.env.example` 은 비어 있지 않습니다

「아직 404」 주석은 **다섯 곳** 고쳤습니다. 네 곳 짚어 주셨고 `src/api/records.js:270` 이 하나 더 있었습니다.

`daysRecorded` 와 `topState` 가 아직 `0 · null` 로 온다는 건 **주석에 그대로 남겼습니다.** 그건 사실이고, 지우면 다음 사람이 「왜 0이지」를 다시 찾게 됩니다.

### `.env.example`

**비어 있지 않습니다.** 3,394바이트이고 `VITE_API_BASE` 를 포함해 전부 들어 있습니다.

```
커밋   818b367  feat: 백엔드 https 전환 + 홈·기록 3건 실연동
내용   VITE_API_BASE=https://fromnowon.duckdns.org/api/v1
       + CORS 허용 출처 목록, 프록시/preview 안내, live.js 묶음 설명
```

`818b367` 은 리뷰 기준인 `406ab9a` **보다 앞선** 커밋이라 그 시점에도 들어 있었습니다. 혹시 `.env` 나 `.env.development`(둘 다 gitignore 대상이라 저장소에 없습니다)를 보신 게 아닌지 확인 부탁드립니다.

이번에 CORS 허용 목록에 배포 주소도 넣어 뒀습니다.

---

## 4. ★ 회원가입 흐름이 리뷰 이후 바뀌었습니다

리뷰에 「signup 시도 → 409 면 같은 비밀번호로 login」으로 적혀 있는데, **그 뒤에 바꿨습니다.**

문제가 하나 있었습니다 — **이미 가입한 사람에게도 「비밀번호를 설정하세요」와 닉네임 칸이 먼저 뜹니다.** 그 사람은 설정할 게 없습니다.

**지금은 로그인을 먼저 시도합니다.**

```
1. 비밀번호만 받아 로그인 → 200 이면 끝. 재방문은 대부분 여기서 끝납니다
2. 401 이면 아직 모릅니다 — 없는 계정인지 틀린 비밀번호인지 서버가 일부러 구분해 주지
   않으니까요. 이때 처음 닉네임을 묻습니다
3. 가입 → 201 이면 새 계정이었던 것
        → 409 면 계정이 있다는 뜻이니 아까 401 은 비밀번호가 틀린 것 →
          로그인 화면(B02-1)으로 보내 다시 치게 합니다
```

**말씀하신 위험은 그대로 피했습니다.** 401 을 「새 계정이구나」로 읽고 곧장 가입시키지 않습니다 — 3단계에서 409 를 반드시 확인하기 때문에, 비밀번호를 잘못 친 사람에게 엉뚱한 계정이 만들어지지 않습니다.

문구도 갈립니다.

| 상황 | 제목 | 버튼 |
|---|---|---|
| 처음 | 비밀번호를 입력하세요 | 다음 |
| 401 뒤 | 처음 오셨네요 / 앱에서 부를 이름을 알려주세요 | 가입하고 시작하기 |

세 갈래 다 확인했습니다 — 재방문+맞는 비밀번호(login 만 호출), 새 계정(login→이름→signup), 재방문+틀린 비밀번호(login→signup 409→로그인 화면).

`nextScreen()` 을 좋게 봐주셔서 감사합니다. 게스트 시작·가입·로그인 셋 다 이걸 탑니다.

---

## 5. ⚠️ `migrated` — 요약과 코드가 다릅니다

메시지 요약에는 이렇게 적혀 있는데,

```
201  { token, userType: "member", migrated: true }
```

보내주신 `AuthService` 코드는 **객체**를 돌려줍니다.

```java
new AuthSignupRes.Migrated(counts.getItems(), counts.getCheckins(), counts.getLogs())
```

`if (res.migrated)` 로 쓰면 **0건이어도 참**이라 「데이터를 옮겨왔다」로 잘못 읽힙니다. **코드 쪽이 맞다고 보고** 목을 `{ items, checkins, logs }` 로 맞춰 뒀습니다. 요약이 맞는 거면 알려주십시오.

---

## 6. 아직 답을 못 받은 것 여섯

앞선 회신에 적었는데 아직 답이 없는 것들입니다. 급한 순서로 적습니다.

### ① 판정을 동시에 두 번 부르면 하나가 500 입니다

같은 `checkinId` 로 동시 요청하면 재현됩니다. 사용자가 빠르게 두 번 누르면 배포본에서도 납니다.

```
[요청2 HTTP 200] [요청1 HTTP 500]  → INTERNAL_ERROR
```

이미 만든 판정을 돌려주는 쪽이 자연스러워 보입니다.

### ② `POST /today/complete` 를 두 번 부르면 기록이 두 줄 남습니다

```
POST /today/complete → 200  lg_b3dbd045…
POST /today/complete → 200  lg_88795cf4…   ← 새 logId
```

`ALREADY_COMPLETED` 는 `reroll` 에만 걸리고 `complete` 에는 안 걸립니다. 화면에서는 `status: "done"` 이면 타이머를 안 띄우도록 막아 뒀지만, 서버에서도 한 번 봐주시면 좋겠습니다.

### ③ 「실행 정도」를 저장하는 API 가 없습니다

시안 H03 의 「추천대로 했어요 / 조금 바꿨어요 / 거의 못 했어요」입니다. `subtract/history` 에 그 값이 없고, **G02 덜어내기 기록 화면의 「기록 저장하기」가 아무 데도 안 보냅니다.** 지금은 화면에서 뺀 상태입니다. API 를 만들지, 기능을 접을지 정해 주십시오.

### ④ `cautions[].keywords` 를 화면이 어디서 받습니까

서버가 문장에서 안 뽑는 건 이해했는데, **저희가 뽑아도 똑같이 지어내는 것**입니다. 관리 맥락 입력 화면이 아직 없어서 시안 나올 때 같이 정해야 할 것 같습니다. 자주 쓰는 클리닉 안내를 몇 개 만들어 두고 고르게 하는 쪽이 맞아 보입니다.

### ⑤ B02 의 닉네임 입력 줄이 시안에 없습니다

가입에 필수라(없으면 400) 제가 한 줄 넣었습니다. 지금은 401 을 받은 뒤에만 펼칩니다. 시안 확인 부탁드립니다.

### ⑥ 홈 카드의 「루틴 따라하기」 라벨

이제 사례를 여는 동작인데 이름이 약속을 과하게 합니다. 시안 문구라 임의로 못 바꿉니다. 「이야기 보기」 같은 쪽이 맞을지 원석님 확인 부탁드립니다.

---

## 7. 데이터가 없어 화면에서 뺀 것 — 이대로 둘지 확인

전부 **주는 API 가 없어서** 「아직 없어요」로 비워 둔 자리입니다. 지어내지 않았습니다.

| 자리 | 상태 |
|---|---|
| 이어가는 첫 발자국 카드 (홈·기록·첫발자국관리) | `GET /home` 에 `streak` 이 없음 |
| 완료한 첫 발자국 목록 | 주는 API 없음 |
| 주간 컨디션 그래프 (기록 H02 · 컨디션 허브) | 주는 API 없음 |
| 마이 「첫 발자국 N개 진행 중」 | 주는 API 없음 |
| 최빈 컨디션의 징후 꼬리표 | `topSignals` 가 서버에 없음(확인함) |

**앞의 넷은 `NOW-STEP-005` 를 보고 나니 「원래 안 만드는 것」일 수도 있겠다 싶습니다.** 맞다면 화면에서 자리째 빼는 게 낫습니다. 확인 부탁드립니다.

---

## 8. 실계정 가입 한 번은 사람이 해봐야 합니다

저는 계정을 만들지 않고 요청을 가로채는 방식으로 분기만 확인했습니다. 아래 둘은 서버 왕복이 있어야 확실합니다.

- `migrated` 로 게스트 항목이 실제로 넘어오는지
- 회원 상태에서 닉네임 변경(`PATCH /me` 200)과 상단바 반영

배포본에서 한 번 해보시고 어긋나면 알려주십시오.

---

## 배포

```
https://fromnowon.netlify.app
```

CORS 등록해 주신 뒤로 데이터까지 정상입니다. `f85b667` 까지 올라가 있습니다.
게스트 세션이 브라우저에 남아 처음부터 안 보이면, 개발자도구 → Application → Local Storage → `now.session` 을 지우고 새로고침하시면 됩니다.

고생 많으셨습니다.
