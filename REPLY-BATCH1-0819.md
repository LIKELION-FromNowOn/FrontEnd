# 1차 전달분 확인 결과 — 2026-08-19

**보내는 사람** 황인서 (프론트)
**받는 사람** 송원석
**대상** `FE-BATCH1-0819.md` 5장 「알려 주실 것 세 가지」

> **결론 — ①~⑤ 다섯 개 전부 통과했습니다.** 다만 ②는 CORS 허용 목록이 아니라 **개발 프록시로 우회해서** 통과한 것이라, 배포 전에는 반드시 서버 쪽 설정이 필요합니다.

---

## 1. ①~⑤ 확인 결과

| # | 무엇 | 결과 | 실측 |
|---|---|---|---|
| ① | 서버에 닿는가 (curl) | **통과** | `HTTP/1.1 200` · `"ok":true` |
| ② | 브라우저에서 닿는가 | **통과** (프록시 우회) | 아래 참고 |
| ③ | 봉투를 벗기는가 | **통과** | `data.token` → `eyJhbGciOiJIUzI1NiJ9.eyJ…` |
| ④ | 토큰이 헤더에 실리는가 | **통과** | `Authorization: Bearer …` 로 요청 → 200 |
| ⑤ | 실패 응답을 파싱하는가 | **통과** | `{"ok":false,...,"error":{"code":"INTERNAL_ERROR",...}}` 를 읽어 `ApiError.code` 로 넘김 |

**`POST /auth/guest` 는 지금 화면에서 실제로 동작합니다.** 게스트 토큰을 받아 저장하는 것까지 확인했습니다. `live.js` 에서 그 한 줄만 켰습니다 (36건 중 1건 실연동).

### ②에 대한 보충 — 프록시로 우회했습니다

**5173 이 다른 프로세스에 점유돼 있어 포트를 옮길 수 없었습니다.** 그래서 기다리지 않고 개발 서버에 프록시를 넣었습니다.

```
브라우저 → localhost:5174/api/v1/...  →  vite 가 서버끼리 전달  →  1.201.116.42:8080
```

**서버 간 호출이라 CORS 가 없습니다.** 덕분에 오늘 관문 검증을 끝냈습니다.

> ⚠️ **이건 개발 전용입니다.** 배포본(Netlify)에는 프록시가 없어서 **CORS_ORIGINS 에 배포 주소가 없으면 화면에서 전부 실패합니다.** 배포 주소가 정해지는 대로 바로 알리겠습니다.

### 프록시 하면서 만난 것 — 참고하시라고

처음에 프록시를 켰더니 서버가 **`Invalid CORS request`** 로 거부했습니다.
프록시가 브라우저의 `Origin: http://localhost:5174` 를 **그대로 전달**하기 때문이었습니다.

전달할 때 `Origin` 을 허용된 `http://localhost:5173` 으로 바꾸도록 해서 해결했습니다.
**CORS 에 저희 주소가 추가되면 그 한 줄은 지웁니다.**

---

## 2. dev 서버 포트 — **5174** 입니다

**`http://localhost:5174` 를 `CORS_ORIGINS` 에 넣어 주십시오.**

5173 은 제 노트북에서 다른 프로세스가 쓰고 있어서 비울 수가 없습니다.
**포트를 `strictPort` 로 5174 에 고정**해 뒀습니다. 빈 포트를 자동으로 찾게 두면 실행할 때마다 주소가 바뀌어서 CORS 등록이 의미가 없어지기 때문입니다. **이제 흔들리지 않습니다.**

추가되면 프록시 우회를 지우고 직접 호출로 되돌리겠습니다. 급하지는 않습니다 — 지금도 개발은 막히지 않습니다.

---

## 3. base URL — **한 곳에 있습니다**

`src/api/config.js` 의 `API_BASE` **한 줄**입니다. 36건 전부 이 값을 통해서만 주소를 만듭니다.

```js
// src/api/config.js
const DEFAULT_BASE = 'http://1.201.116.42:8080/api/v1'
export const API_BASE = (import.meta.env.VITE_API_BASE || DEFAULT_BASE).replace(/\/$/, '')
```

**도메인 전환 때 바꿀 곳은 두 군데뿐입니다.**

| 무엇 | 왜 |
|---|---|
| `.env.production` 의 `VITE_API_BASE` | 배포 빌드가 쓰는 값 |
| `config.js` 의 `DEFAULT_BASE` | `.env` 가 gitignore 라 다른 사람 환경·CI 에서 비어 있을 때의 기본값 |

**`http` → `https` 전환도 문제없습니다.** 말씀하신 대로 올라가는 방향이라 브라우저가 막지 않습니다.

---

## 곁들여 — 저희 쪽 준비 상태

**목 36건을 실서버와 같은 봉투로 맞췄습니다.** 이게 이번 작업의 핵심입니다.

```js
ok(data)              // { ok:true,  data, error:null }
fail(code, message)   // { ok:false, data:null, error:{code,message} }
```

목도 실연동과 **똑같은 코드 경로**를 탑니다. 목에서 `fail('CANNOT_REVERT_EXCLUDED', ...)` 를 돌려주면 실서버와 똑같이 `ApiError` 가 납니다. 그래서

- 서버 없이도 **에러 화면을 지금 확인**할 수 있고
- 스위치를 켤 때 **파싱 코드를 손댈 일이 없습니다**

`live.js` 는 요구사항 전달 묶음 그대로 나눠 뒀습니다. **「이 묶음 됐습니다」만 올려 주시면 그 묶음만 켭니다.**

```
auth 13 · master 3 · subtract 3 · today 11 · log 3 · note 1 · safety 2
```

에러 코드는 주신 **15종을 상수로** 넣었고, 아직 안 나오는 3종(`NO_CHECKIN` · `NO_EVALUATION` · `TIMER_ALREADY_RUNNING`)도 방어는 그대로 뒀습니다.
**`ITEM_NOT_SELECTED` · `ITEM_EXCLUDED` 두 개는 넣지 않았습니다.**

`credentials` 는 보내지 않습니다. 확인했습니다.

### 5장 ① 함정도 실제로 확인했습니다

```
GET /api/v1/nope  →  500  {"ok":false,"data":null,"error":{"code":"INTERNAL_ERROR",...}}
```

말씀대로 404 가 아니라 500 이 옵니다. **500 이 나오면 URL 부터 의심하겠습니다.**

---

## 제가 지금 기다리는 것 — 하나뿐입니다

| 무엇 | 언제 필요한가 |
|---|---|
| **`CORS_ORIGINS` 에 `http://localhost:5174`** | 급하지 않음. 프록시로 개발은 진행됩니다 |
| **배포 주소 확보 후 CORS 등록** | ⚠️ **배포 전 필수.** 없으면 배포본에서 전부 실패합니다 |

**2차(마스터 3건 + 판정 3건)는 준비돼 있습니다.** 올려 주시는 대로 스위치만 켜겠습니다.
