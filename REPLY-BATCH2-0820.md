# 2차 전달분 확인 결과 — 2026-08-20

**보내는 사람** 황인서 (프론트)
**받는 사람** 송원석
**대상** `FE-BATCH2-0820.md` · `FE-HANDOFF-2-0820.md`

> **요청하신 목 4군데는 전부 고쳤고 푸시했습니다.**
> **다만 「켜셔도 됩니다」라고 하신 체크인 2건은 켜지 않았습니다.** 켜기 전에 실측했더니 조용히 틀리는 자리가 있었습니다. 2장에 적었습니다.
> **그리고 부탁이 하나 있습니다 — `GET /signals` 컨트롤러입니다.** 그거 하나면 체크인 2건이 같이 켜집니다.

---

## 1. 목 4군데 — 전부 수정했습니다

| # | 무엇 | 어떻게 고쳤나 |
|---|---|---|
| ① | 되돌리기에 본문이 없었음 | `{ evaluationId }` 를 실어 보냅니다 |
| ② | 응답이 `reverted` 였음 | `persisted` + `summary` 로 맞췄습니다 |
| ③ | `NO_EVALUATION` 409 | `EVALUATION_NOT_FOUND` 404 로 정정. **`ERROR` 상수에서 `NO_EVALUATION` 을 지웠습니다** |
| ④ | `getCheckinsLatest` 가 `signalIds` 를 줬음 | 뺐습니다. 명세서에 없고 서버도 안 줍니다 |

**②를 짚어 주신 게 특히 도움이 됐습니다.** `summary` 가 같이 온다는 걸 몰랐으면 되돌린 뒤에 개수를 다시 부르는 코드를 넣었을 겁니다.

### 같이 맞춘 것 — 실측 형태 그대로

- **판정 응답 전체** — `evaluationId` · `checkinId` · `createdAt` · `state` · `judgeStrength` · `filter` · `generatedBy` · `summary` · `results[]`
- `results[]` 안쪽도 전부 — `name` · `frequency` · `evidenceLevel` · `floor` · `floorApplied` · `reverted` · `revertable` · `excludedBy` · `noteSent` · `daysLeft`
- **`summary` 는 필터를 걸어도 전체 기준**이라는 것을 코드 주석에 박아 뒀습니다. 나중에 누가 「필터 걸었는데 숫자가 안 바뀌네」로 고치지 않도록
- **체크인** — 요청 `{state, signalIds, customSignals}` / 응답 `signalScore` · `threshold` · `maxScore` · `transitionProposed` · `proposedState` · `reasons` · `judgeStrength`
- **오류 17종** — `METHOD_NOT_ALLOWED` · `NO_CHECKIN` 추가
- **`revertable` 을 우선 씁니다.** 서버가 직접 알려주니 프론트가 판정으로 추측하지 않습니다. 없을 때(목)만 추론합니다

### 500 결함 해소도 반영했습니다

경로 오타를 500 으로 의심하던 코드를 **`NOT_FOUND` 기준**으로 바꿨습니다.
말씀하신 대로 이제 **`NOT_FOUND` 면 경로가 틀린 것**, **`EVALUATION_NOT_FOUND` 면 판정이 없는 것**으로 갈라집니다. 이게 디버깅에 실제로 쓸모 있습니다.

---

## 2. ⚠️ 체크인 2건을 안 켠 이유 — 200 인데 점수가 0 입니다

켜기 전에 실서버로 두 번 찔러 봤습니다.

```bash
# 서버가 아는 id
{"state":"low","signalIds":["sig_01","sig_04"]}
→ 200  signalScore 5 · threshold 5 · transitionProposed true   ✅

# 우리 화면이 지금 보내는 값(한글 이름)
{"state":"low","signalIds":["건조함","잠이 부족해요"]}
→ 200  signalScore 0 · transitionProposed false                ⚠️
```

**오류가 아니라 200 에 `signalScore: 0` 이 옵니다.**

### 왜 이렇게 되는가

우리 징후 목록은 **시안 문구 약 27개**입니다. 서버 징후는 **14개이고 id 가 `sig_01`~`sig_14`** 입니다.
**`GET /signals` 컨트롤러가 없어서 id 를 받아올 방법이 지금 없습니다.**

### 켰으면 어떻게 보였을까

- 전환 제안이 **한 번도 안 뜹니다**
- 그런데 화면은 정상으로 보입니다. 오류도 없고 로딩도 끝납니다
- **「연동은 됐는데 왜 전환이 안 뜨지」**로 보여서, 원인이 징후 id 라는 걸 알아내는 데 시간이 갑니다

> 8/20 문서에 **「오늘 아침까지 `signalScore` 가 항상 0 이었습니다」**라고 적어 주신 것과 증상이 똑같습니다.
> 그때는 서버가 원인이었고 이번엔 프론트가 보내는 값이 원인인데, **화면에서는 구분이 안 됩니다.**

### 그래서 한 것

- **두 줄을 끄고 사유를 코드에 남겼습니다.** 나중에 「왜 안 켜져 있지」 하고 그냥 켜지 않도록
- **임시 방어를 넣었습니다** — 서버가 아는 이름만 `signalIds` 로 보내고, 모르는 것은 `customSignals` 로 넘깁니다. 마스터가 붙으면 자동으로 전부 id 로 갑니다. **코드를 다시 고칠 필요가 없습니다**

---

## 3. 부탁 — `GET /signals` 하나만 먼저

**이거 하나면 체크인 2건이 같이 켜집니다.** 지금 실연동을 막고 있는 유일한 것입니다.

| | |
|---|---|
| **막고 있는 것** | 상태 체크 실연동 · 전환 제안 화면 · `reasons` 3줄 |
| **필요한 것** | `GET /signals` 컨트롤러 (김민정 님) |
| **데이터** | **이미 DB 에 있습니다** — 14건 · 가중치 합 25 |
| **프론트 준비** | 끝. 붙는 즉시 스위치 두 줄만 켭니다 |

카테고리·관리 항목보다 **징후가 먼저**입니다. 앞의 둘은 없어도 목으로 화면이 그려지는데, 징후는 **없으면 실연동 자체가 잘못된 결과를 냅니다.**

> 마스터 3건을 한꺼번에 못 주셔도 됩니다. **`/signals` 하나만 먼저 얹어 주시면** 그날 바로 전환 제안까지 실서버로 시연할 수 있습니다.

---

## 4. 물어보신 것 두 가지

### ① 카테고리 아이콘 — **프론트가 갖는 게 맞습니다**

**DB 컬럼 늘리지 마십시오.** 판단 잘하셨습니다.

아이콘은 화면 표현이고 서버가 알 이유가 없습니다. 지금처럼 프론트에 두면 디자인이 바뀔 때 저희 쪽만 고치면 되고, 서버에 두면 시안이 바뀔 때마다 배포가 필요해집니다.

### ② 최근 상태 조회에 징후 목록 — **필요 없습니다. 명세서 고치지 마십시오**

컨디션 화면은 **매번 새로 고르는 구조**라 지난 선택을 복원할 자리가 없습니다.
동결 하루 전에 명세서를 고칠 이유가 못 됩니다.

---

## 5. 지금 실연동 상태

| 묶음 | 실연동 | 비고 |
|---|---|---|
| `POST /auth/guest` | ✅ **켬** | 화면에서 실제로 돕니다 |
| 체크인 2건 | ⏸ 대기 | **서버는 정상.** `/signals` 를 기다립니다 |
| 판정 3건 | ⏸ 끔 | 오류 봉투 파싱만 확인하고 껐습니다 (아래) |
| 나머지 30건 | 목 | 그대로 둡니다 |

### 판정 3건 — 확인 결과

말씀하신 대로 오류가 옵니다. **화면이 삼키지 않고 읽습니다.**

```
POST /subtract/evaluate  → 400 MIN_ITEMS_REQUIRED  "관리 항목을 3개 이상 선택해 주세요"
GET  /subtract/result    → 404 EVALUATION_NOT_FOUND
없는 경로                 → 404 NOT_FOUND
토큰 없이                 → 401 UNAUTHORIZED
```

**`PUT /me/items` 가 들어오면 그날 바로 켜겠습니다.** 프론트는 준비돼 있습니다.

---

## 6. dev 포트 — 아직 `5174` 입니다

1차 회신에도 적었는데 다시 남깁니다. **`http://localhost:5174`** 를 `CORS_ORIGINS` 에 넣어 주십시오.

5173 은 제 노트북에서 다른 프로세스가 쓰고 있어 비울 수 없어서, 포트를 `strictPort` 로 5174 에 고정해 뒀습니다. **이제 실행할 때마다 흔들리지 않습니다.**

지금은 개발 서버 프록시로 우회하고 있어 **개발은 막히지 않습니다.** 급하지 않습니다.
다만 **배포본에는 프록시가 없어서, 배포 주소가 CORS 에 없으면 화면에서 전부 실패합니다.** 배포 주소 나오는 대로 바로 알리겠습니다.

---

## 7. 아직 열려 있는 것 — 기록 탭 API

어제 올린 `REPLY-RECORDS-API-0820.md` 건입니다. **아직 답을 못 받았습니다.**

기록 탭 3화면(H01·H02·H03)은 완성돼 있는데 **붙일 API 가 명세서 36건에 없습니다.**

- H01 이 쓰는 「기록한 날·덜어낸 날·이어간 날」 일수 3종, 최빈 컨디션, 자주 덜어낸 항목
- H03 의 **덜어내기 기록** 저장·조회 — `GET /logs` 는 「완료한 행동」이라 다른 데이터입니다

**제 의견은 「기록 탭은 목으로 발표」입니다.** 발표 계정에는 어차피 데이터가 며칠치 없어서 실연동이든 목이든 화면이 같고, 오늘 남은 시간은 `/signals` 와 `PUT /me/items` 에 쓰는 편이 낫습니다.

**그렇게 정하셔도 알려만 주십시오.** 정해지지 않은 채로 동결을 넘기는 것만 피하고 싶습니다.
