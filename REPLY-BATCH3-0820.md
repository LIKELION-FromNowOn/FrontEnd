# 3차 전달분(8건) 확인 결과 — 2026-08-20

**보내는 사람** 황인서 (프론트)
**받는 사람** 송원석 · 김민정
**대상** 관리 항목 3건 · 오늘의 행동 5건 · 덜어내기 3건

> **8건 다 붙였고, 화면에서 처음부터 끝까지 한 번 돌려봤습니다.** 게스트 → 항목 저장 → 상태 체크 → 판정 → 되돌리기 → 오늘의 행동 → 타이머 → 완료까지 실서버로 갑니다.
> **다만 안내에 없던 것이 세 가지 있어서 그게 없었으면 전부 400 이었을 겁니다.** 1장에 적었습니다.
> **그리고 서버 쪽으로 보이는 것 두 가지를 찾았습니다.** 3장입니다. 재현 명령까지 같이 넣었습니다.

---

## 1. 안내에 없었는데 이게 없으면 안 돌아가는 것 3개

### ① 마스터 3건이 이미 살아 있습니다 — 그리고 이게 없으면 저장이 안 됩니다

`GET /categories` · `GET /care-items` · `GET /signals` 셋 다 200 이고 시드도 들어가 있었습니다(7분류 · 32항목 · 징후 14건). 「8개」 목록에도, 「목으로 두실 것」 목록에도 없어서 확인차 찔러 봤다가 알았습니다.

**중요한 건 이게 선택이 아니라는 점입니다.** `PUT /me/items` 는 마스터의 `itemId` 를 받습니다.

```bash
# 이름으로 보내면
{"items":[{"name":"폼 클렌저","frequency":"daily"}, …]}
→ 400 VALIDATION_FAILED  "items[1].itemId — must not be blank"

# id 로 보내야
{"items":[{"itemId":"cr1","frequency":"daily"}, …]}
→ 200 {"saved":3,"needsRejudge":true}
```

즉 **마스터를 안 켜면 관리 항목 저장부터 막히고, 그 뒤 판정·오늘의 행동이 통째로 안 돕니다.** 그래서 마스터 3건도 같이 켰습니다.

덕분에 화면 내용도 바뀌었습니다. 저희 시안은 피부 항목만 21건이었는데 서버는 수면·운동·식사·마음·일상·건강까지 32건이라, **관리 항목 선택 화면이 이제 서비스 전체 모습으로 나옵니다.** 이게 맞는 방향이면 그대로 두겠습니다.

### ② `POST /subtract/evaluate` 는 `checkinId` 가 필수입니다

```bash
POST /subtract/evaluate  {}
→ 400 VALIDATION_FAILED  "checkinId — checkinId 가 필요합니다"
```

덜어내기 화면은 상태 체크 화면과 이어져 있지 않아서 그 값을 들고 있지 않았습니다. `GET /checkins/latest` 로 먼저 찾아서 붙여 부르는 것으로 바꿨습니다(그래서 이 1건도 같이 켰습니다). 화면끼리 값을 들고 다니면 새로고침 한 번에 끊깁니다.

### ③ 징후는 반드시 `id` 로 보내야 합니다 — 그리고 이제 됩니다

지난번에 「이름을 보내면 오류 없이 0 점이 된다」고 말씀드렸던 건입니다. `GET /signals` 를 켜니 실제 id 가 들어와서 해결됐습니다.

```bash
signalIds: ["sig_04","sig_01","sig_13"]  → signalScore 7 (임계값 5), transitionProposed true
```

**`ERROR` 상수에서 지웠던 `NO_EVALUATION` 은 되살렸습니다.** 실측해 보니 둘 다 나오고 뜻이 다릅니다 — 화면이 가야 할 곳도 다릅니다.

| 상황 | 응답 | 화면 |
|---|---|---|
| 판정을 아직 안 함 (`GET /today`) | 409 `NO_EVALUATION` | 덜어내기로 보냄 |
| 판정 결과 조회인데 없음 (`GET /subtract/result`) | 404 `EVALUATION_NOT_FOUND` | 오류 안내 |
| 판정은 했는데 남은 후보 없음 (`GET /today`) | 200 `data: null` | 첫 발자국 카드 |

이 셋을 갈라서 처리합니다. 맞게 이해한 것인지 한 번만 확인 부탁드립니다.

---

## 2. 확인한 것 — 말씀하신 대로였습니다

| 확인 항목 | 결과 |
|---|---|
| `GET /me/items` 가 배열 그대로 (`{items:…}` 아님) | ✅ `[{itemId,name,category,frequency,floor,custom}]` |
| `POST /me/items/custom` 201 · `cu_` 접두사 | ✅ `evidenceLevel` · `interpretedBy` 까지 그대로 |
| 직접 입력을 `PUT` 에 실으면 400 | ✅ `"존재하지 않는 항목이 포함되어 있습니다"` — 아예 안 싣도록 걸러서 보냅니다 |
| `DELETE` 로 3개 미만 되면 막힘 | ✅ 400 `MIN_ITEMS_REQUIRED` |
| `needsRejudge` | ✅ `PUT` · `DELETE` 둘 다 옵니다 |
| 거절 사유 `time`·`fit`·`none` | ✅ 옛 5종(`no_time` 등)은 400. **시안 5개를 3개로 접었습니다** |
| 다시 받기 하루 3회 | ✅ 4번째에 429 `REROLL_LIMIT`, `rerollLeft` 도 0까지 정확히 내려옵니다 |
| 없는 행동 | ✅ 404 `ACTION_NOT_FOUND` |
| `durationSec` 을 상수로 두지 말 것 | ✅ 목 값으로 대신 그리던 자리를 **전부 없앴습니다**(아래 4장) |
| 판정에 AI | ✅ `generatedBy: "llm"`, 근거 문장이 항목마다 다릅니다 |

**「이유가 남는다」는 점 때문에 거절 사유를 3개로 줄였습니다.** 시안엔 「이미 했어요」가 있었는데, 그건 거절이 아니라 완료라서 뺐습니다(`POST /today/complete` 쪽 일입니다).

---

## 3. ⚠️ 서버 쪽으로 보이는 것 2개

### ① 판정을 동시에 두 번 부르면 하나가 500 입니다

개발 모드에서 화면이 이펙트를 두 번 실행하는데(React StrictMode), 그때 재현됐습니다. 사용자가 빠르게 두 번 누르면 배포본에서도 납니다.

```bash
CK=…  # 같은 checkinId
for i in 1 2; do
  curl -s -X POST "$B/subtract/evaluate" -H "$A" -H 'Content-Type: application/json' \
       -d "{\"checkinId\":\"$CK\"}" -w "[$i %{http_code}] " &
done; wait

[요청2 HTTP 200] [요청1 HTTP 500]
→ {"code":"INTERNAL_ERROR","message":"잠시 후 다시 시도해 주십시오"}
```

같은 checkinId 로 두 번 들어오면 **이미 만든 판정을 돌려주는 쪽**이 자연스러울 것 같습니다. 프론트에서 막을 수도 있지만, 두 탭을 열어도 나는 일이라 서버에서 한 번 봐주시면 좋겠습니다.

### ② `POST /today/complete` 를 두 번 부르면 기록이 두 줄 남습니다

```bash
POST /today/complete  → 200  logId lg_b3dbd045a4d04a369faa
POST /today/complete  → 200  logId lg_88795cf43fdf4707a81f   ← 새 logId
```

`ALREADY_COMPLETED` 는 **`reroll` 에만** 걸리고 `complete` 에는 안 걸립니다. 기록이 두 번 쌓이면 「완료한 것만 남긴다」는 전제가 흔들려서, 프론트에서는 일단 **`status: "done"` 이면 타이머 화면을 안 띄우도록** 막아 뒀습니다. 다만 이건 화면에서만 막는 것이라 서버에서도 한 번 봐주시면 좋겠습니다.

---

## 4. 저희 쪽에서 같이 고친 것

- **판정 결과를 `items` 로 읽고 있었습니다.** 실제 키는 `results` 라서, 서버가 붙어도 계속 목이 보이던 자리였습니다. 이번에 실연동하고 나서야 드러났습니다
- **되돌리기에 `evaluationId` 를 안 실어 보내고 있었습니다.** 화면에서 빠져 있었습니다
- **타이머 화면이 응답을 못 받으면 목 행동을 그리고 있었습니다.** 4분짜리 화면을 띄워놓고 서버엔 없는 id 로 시작하는 꼴이라 없앴습니다. 이제 「덜어내기 먼저」 · 「오늘 드릴 케어가 없어요」 · 오류를 갈라서 띄웁니다
- **관리 항목을 항목명이 아니라 `itemId` 로 들고 있도록** 바꿨습니다(1장 ①)
- 직접 입력이 화면에만 있고 아무 데도 안 보내지고 있었습니다. `POST /me/items/custom` · `DELETE` 로 연결했습니다
- 응답의 `category` 는 코드(`care`·`move`)라 그대로 띄우면 「move」가 나옵니다. `GET /categories` 로 이름을 찾아 띄웁니다

---

## 5. 아직 목으로 둔 것 — 말씀하신 그대로입니다

회원가입 · 로그인 · 로그아웃 · 내 정보 · 프로필 · 상태 전환 / 홈 · 기록 2건 · 관리 맥락 2건 · 예정 3건 · 첫 발자국

**하나만 부탁드립니다 — `GET /me` 입니다.** 「오늘의 컨디션」 허브 화면에서 오늘 답한 컨디션을 되살려야 하는데 지금은 화면에 고정값이 박혀 있습니다. 관리 항목 쪽은 `GET /me/items` 로 이미 살아났습니다.

---

## 6. 덧붙임 — `PUT /me/care` 의 `noteLines` · `cautions[].keywords` (같은 날 추가 전달분)

붙였습니다. 노션 `NOW-NOTE-002` 도 같이 봤습니다.

**그리고 확인해 보니 관리 맥락 2건 · 예정 3건 · 안내문 원문 · 케어 코치가 전부 살아 있었습니다.** 「목으로 두실 것」 목록에 있던 것들인데 마스터 때와 같은 상황이라, 실측한 것만 켰습니다. 실제로 끝까지 돕니다.

```
PUT /me/care  (noteLines 2문장 + keywords)   → saved: 2, daysLeft 1·5 계산됨
POST /me/plans "클라이밍 약속"                → conflict true,  sent 1
POST /me/plans "도서관 가기"                  → conflict false          ← 헛걸림 없음
POST /coach/ask "오늘 클라이밍 가도 되나요"    → level no · 원문 1번째 문장
GET  /me/care/note                           → rules[].kw 가 저장한 keywords 그대로
```

말씀하신 **「조회해서 다시 저장할 때 값을 잃는」 사고**는 이렇게 막았습니다.

- `toCareSaveShape(care, noteLines)` 를 거치지 않으면 저장 본문을 못 만들게 했습니다. 조회 응답을 그대로 넣으면 `keywords` 가 따라갑니다(왕복 테스트로 확인)
- `keywords` 가 빈 주의사항이 있으면 **개발 모드에서 콘솔로 경고**합니다. 저장은 200 이라 화면에서는 잘 된 것처럼 보이는 자리라서요
- `sent` 가 `noteLines` 범위를 벗어나도 경고합니다. **이건 서버도 막아주시더군요** — `"5번째 문장이 noteLines 에 없습니다"`. 좋았습니다

### 켜면서 찾은 화면 결함 하나 (저희 쪽)

**케어 코치가 「안내문에 없습니다」를 받으면 화면이 죽습니다.** 그 응답에는 `level` 이 아예 없고 `basis` 도 `{type:"none"}` 뿐인데, 화면이 `level` 로 배지를 찾다가 터집니다. 안내문에 없는 질문은 흔해서 실제로 자주 났을 겁니다.

배지와 근거 줄을 **값이 있을 때만** 띄우도록 고쳤습니다. 판단할 근거가 없다는 뜻이라 아무 배지나 붙이면 서버가 하지 않은 말을 화면이 하게 됩니다.

### 확인 하나 부탁드립니다 — `keywords` 는 누가 만듭니까

서버가 문장에서 뽑지 않는다는 건 이해했습니다. 다만 **저희도 지어내면 같은 문제**라, 화면이 이 값을 어디서 받아야 하는지가 아직 비어 있습니다. 지금 관리 맥락 입력 화면 자체가 없어서 시안이 나올 때 같이 정해야 할 것 같습니다.

떠오르는 건 셋입니다.

1. 사용자가 낱말까지 직접 적는다 — 정확하지만 부담이 큽니다
2. 프론트가 고정 사전으로 문장에서 뽑는다 — 결국 「지어내는 쪽」이라 같은 문제입니다
3. 자주 나오는 클리닉 안내를 몇 개 만들어 두고 고르게 한다 — 낱말이 미리 붙어 있습니다

**저희는 3번이 맞다고 봅니다.** 어느 쪽인지 정해 주시면 그대로 붙이겠습니다.

---

## 7. CORS 5174 — 확인했습니다. 우회하던 줄도 지웠습니다

말씀하신 그대로 나옵니다.

```
OPTIONS (PUT 프리플라이트)  Origin: localhost:5174  → 200 · Allow-Origin: http://localhost:5174
GET  /care-items                                    → 200
Origin: evil.example.com                            → 403
localhost:5173                                      → 200 (그대로 살아 있음)
```

브라우저에서도 확인했습니다. **배포 빌드를 띄워서 절대 주소로 직접 부르는 경로**까지 봤습니다 — 게스트 발급 · 마스터 조회 · `PUT /me/items` 프리플라이트 전부 통과합니다.

저희 `vite.config.js` 에 **허용된 출처인 척 Origin 을 바꿔 보내던 줄**이 있었습니다. 5174 가 목록에 없던 동안 개발을 이어가려고 넣은 것이었는데, 이제 필요가 없어져서 지웠습니다. 남겨 두면 나중에 5173 이 목록에서 빠지는 날 이유 없이 깨집니다.

### 하나만 알려드립니다 — `vite preview` 기본 포트 4173 은 403 입니다

배포 빌드를 로컬에서 확인할 때 쓰는 포트입니다.

```
Origin: http://localhost:4173  → 403
```

**추가해 달라는 뜻은 아닙니다.** 저희가 preview 포트를 이미 허용된 **5173** 으로 고정했습니다. 이러면 dev(5174)를 켜 둔 채로 배포본을 같이 띄울 수 있고, 개발 서버 프록시로는 확인할 수 없는 실제 CORS 경로를 매번 거치게 됩니다. **이번 같은 일을 배포 뒤가 아니라 배포 전에 잡으려는 것**입니다.

`Access-Control-Allow-Credentials` 는 안 오는 것으로 확인했고, 그래서 요청에 `credentials` 는 계속 안 싣습니다. 그대로 두시면 됩니다.

### 배포 주소는 아직입니다

Netlify 로 올릴 준비(`public/_redirects`)는 돼 있는데 주소가 아직 정해지지 않았습니다. **나오는 대로 바로 알려드리겠습니다.** 9초면 된다고 하셨으니 그때 부탁드립니다.

---

## 8. 기록 탭 — H03 붙였습니다. 「이어간 날」은 뺐습니다

### `GET /subtract/history` 는 **이미 배포돼 있습니다**

「아직 배포 전」이라고 하셨는데 확인해 보니 돌고 있습니다. 마스터·관리 맥락 때와 같은 상황이라, 실측하고 그대로 붙였습니다.

```
GET /subtract/history                          → 200
GET /subtract/history?from=2026-08-14&to=…     → 200
GET /subtract/history?limit=0 · 101 · abc      → 400  "조회 개수가 올바르지 않습니다" 외
GET /subtract/history?from=2026/08/01          → 400  "날짜는 YYYY-MM-DD 입니다"
GET /subtract/history?from=8-31&to=8-01        → 400  "조회 기간이 올바르지 않습니다"
```

**`GET /logs` 와 형태를 맞춰 주신 게 실제로 도움이 됐습니다.** `total` · `hasMore` 가 같아서 두 목록을 한 가지 방식으로 다룹니다. `hasMore` 가 true 면 「전체 N건 중 M건을 보고 있어요」라고 화면에 적습니다 — 조용히 자르지 않으려고요.

「이번 주 · 이번 달」 필터는 `from`·`to` 로 보냅니다. 자세히 보기는 `GET /subtract/result?evaluationId=` 로 열고, **그때 판정을 새로 돌리지 않습니다** — 지난 기록을 보려다 오늘 판정이 생기면 안 되니까요. 실제로 호출 목록에 `POST /subtract/evaluate` 가 안 찍히는 것까지 확인했습니다.

### ★ 「이어간 날」 뺐습니다

H01 히어로의 세 칸이 두 칸이 됐습니다 — 기록한 날 · 덜어낸 날. 왜 뺐는지 이유까지 코드에 적어 뒀습니다. 나중에 누가 되살리자고 할 때 이 결정을 먼저 보게요.

아래 「첫 발자국도 함께 쌓이고 있어요」 카드는 남겨 뒀습니다. 며칠 연속 해냈는지를 세는 자리가 아니라 따라가는 중인 루틴의 진행이라 다른 것으로 봤는데, **아니면 말씀해 주십시오. 그것도 빼겠습니다.**

`GET /logs` · `GET /logs/summary` 는 아직 404 입니다. 민정님 것이라 기다리겠습니다.

### 확인 부탁드릴 것 셋

**① `topState` · `topSubtracted` 의 생김새를 아직 모릅니다.** 이름만 받았고 서버가 404 라 못 봤습니다. 지금은 이렇게 가정하고 두 형태 다 받게 해 뒀습니다.

```json
"topState":      "low"   또는   { "state": "low", "days": 4, "totalDays": 7 }
"topSubtracted": [{ "itemId": "cr4", "name": "고기능성 · 각질 관리", "count": 4 }]
```

**② 최빈 컨디션 카드의 징후 꼬리표는 어디서 옵니까.** 시안에 「조금 예민 · 따가움 · 잠 부족」이 붙는데 알려주신 네 가지에는 없습니다. 서버가 안 주면 그 줄은 안 뜨게 해 뒀습니다 — 화면에서 지어내지 않으려고요.

**③ 「실행 정도」를 저장하는 API 가 없습니다.** 시안 H03 에 「추천대로 했어요 / 조금 바꿨어요 / 거의 못 했어요」 꼬리표가 있는데 `subtract/history` 에 그 값이 없습니다. 저장하는 곳도 없습니다 — **G02 덜어내기 기록 화면의 「기록 저장하기」 버튼이 지금 아무 데도 안 보냅니다.** 일단 H03 에서 그 꼬리표를 뺐습니다. 사용자가 고른 적 없는 답이 자기 기록에 남으면 안 되니까요.

이건 **셋 중 하나로 정하면 될 것 같습니다** — 필요하면 API 를 만들거나, 「실행 정도」 자체를 접거나, 지금처럼 판정 내용만 보여주거나. 정해 주시면 맞추겠습니다.

`GET /subtract/history` 의 API ID 는 명세서에 아직 없어서 `NOW-SUB-004` 로 임시로 붙여 뒀습니다. 다른 번호면 알려주십시오.

---

## 9. 저장소 리뷰 7건 회신

**보신 것이 제 18:10 커밋 시점입니다.** 그 뒤로 오늘 작업분이 커밋 전 상태로 쌓여 있어서, 짚어 주신 7건 중 **5건은 이미 고쳐져 있었습니다.** 대조표입니다.

| # | 무엇 | 상태 |
|---|---|---|
| ① | `getMyItems` — data 가 바로 배열 | ✅ 이미 반영 (`toItemList` 로 두 형태 다 받습니다) |
| ② | `getCategories` — 배열 · `id` | ✅ 이미 반영 |
| ③ | `getCareItems` — 배열 · `id` · `category` | ✅ 이미 반영 |
| ④ | `getSignals` — `id` + 임계값 5종 | ✅ 이미 반영 |
| ⑤ | `saveCare` 에 `cautions` | ✅ 이미 반영 (+ `keywords` 까지) |
| ⑥ | `rerollToday` 에 `{ actionId }` | ✅ 이미 반영 |
| ⑦ | `respondTransition` 에 `checkinId` | **새로 고쳤습니다** |

### ⑦ 는 본문뿐 아니라 **응답도 달랐습니다**

실측해 보니 목이 서버와 두 군데 어긋나 있었습니다.

```
{ accept: true }               → 400 VALIDATION_FAILED
{ checkinId, accept: true }    → 200 { state, accepted, recommendationPaused, needsRejudge }
{ checkinId, accept: false }   → 200 { state, accepted, recommendationPaused,
                                       nextProposalBlockedUntil: "2026-08-23T22:11:37+09:00" }
```

**유예가 일수가 아니라 시각으로 옵니다.** 저희 목에 `cooldownDays: 3` 이 있었는데 서버엔 그런 필드가 없습니다. 남은 날짜를 화면에서 세지 않도록 `nextProposalBlockedUntil` 을 그대로 쓰게 바꿨습니다. 수락하면 `needsRejudge` 가 온다는 것도 목에 반영했습니다.

### ⑤ 는 `keywords` 까지 같이 보냅니다

여기 적어 주신 건 `cautions: [{ itemId, text, sent, dp }]` 인데, 같은 날 노션 `NOW-NOTE-002` 에 `keywords` 가 추가돼 있어서 그쪽까지 맞췄습니다. 6장에 적었습니다. 두 안내가 조금 다른데 **노션 쪽이 최신으로 알고 있습니다** — 아니면 알려주십시오.

`saved` 가 개수라는 것도 반영했습니다. 목도 저장한 주의사항 개수를 돌려줍니다.

### 오류 코드

`TIMER_ALREADY_RUNNING` 지웠습니다. 알려주신 10개 중 `ACTION_NOT_FOUND` · `REROLL_LIMIT` · `ALREADY_COMPLETED` 는 이미 있었고 **나머지 7개를 넣었습니다.**

그중 넷은 실서버로 직접 확인했습니다.

```
PATCH /me  게스트로            → 403 GUEST_FORBIDDEN
PATCH /me  {}                  → 400 NO_FIELDS
POST /state/transition 없는 id → 404 CHECKIN_NOT_FOUND
POST /state/transition 두 번   → 409 NO_PROPOSAL
```

`EMAIL_ALREADY_EXISTS` · `INVALID_CREDENTIALS` · `USER_NOT_FOUND` **셋은 실측하지 않았습니다.** 확인하려면 실제 계정을 만들어야 해서 주신 값을 그대로 적어 뒀습니다. 회원 흐름을 붙일 때 같이 보겠습니다.

### `GET /subtract/history` 와 기록 탭

8장에 적었습니다 — 이미 배포돼 있어서 붙였고, 「이어간 날」도 뺐습니다.

### 스위치는 지금 **30 / 37** 입니다

말씀하신 30건과 맞습니다. 안 켠 일곱은 이렇습니다.

- **가입 · 로그인 · 로그아웃** — 응답 형태를 못 봤습니다(계정을 만들어야 해서). 화면도 아직 목입니다
- **`PATCH /me`** — 경로는 살아 있는데(게스트로 부르면 403) 성공 응답을 못 봤습니다
- **`GET /home` · `GET /logs` · `GET /logs/summary`** — 아직 404 입니다

칭찬해 주신 「왜 안 켰는지 주석에 남기는 것」, 이 일곱 줄에도 그대로 적어 뒀습니다.

---

## 10. `GET /logs` 예고 — 확정 전이라 그대로 뒀습니다

말씀하신 대로 지금은 안 고쳤습니다. **`getLogs` 를 부르는 화면이 아직 없어서** 모양이 바뀌어도 깨질 것이 없고, 확정 전에 맞춰 두면 틀린 모양에 화면을 맞추게 됩니다.

대신 **주석으로 남겨 뒀습니다** — 바뀔 응답 형태, 늘어날 파라미터 넷(`from`·`to`·`categoryId`·`limit`), 그리고 「날짜 묶기와 문구 만들기는 화면 몫」이라는 것까지. 확정 연락 오면 그 주석과 목을 같이 고치겠습니다.

### 날짜 형식은 지금 고쳤습니다 — 이미 붙은 API 에서 일어나던 일이라

`GET /subtract/history` 가 벌써 `2026-08-20` 을 주고 있었고, 기록 탭 두 곳이 그걸 **그대로 화면에 찍고 있었습니다.** 시안은 「8월 20일」입니다.

`src/utils/date.js` 하나로 모았습니다.

```
2026-08-18 → 8월 18일
2026-01-05 → 1월 5일     (0 을 붙이지 않습니다. 시안이 그렇습니다)
날짜가 아닌 값 → 받은 값 그대로   (억지로 해석하면 엉뚱한 날이 뜹니다)
```

`GET /logs` 가 붙을 때도 이걸 그대로 쓰면 됩니다. 연도는 안 붙였습니다 — 기록 탭이 이번 주·이번 달만 보는 화면이라 시안에도 없습니다.

### `period` 기본값은 어긋난 게 아닙니다

`getLogSummary` 의 기본값이 `week` 인 건 맞는데, **부르는 곳이 H01 「이번주 요약」 하나뿐이라 주 단위가 맞습니다.** 게다가 항상 쿼리에 실어 보내서 서버 기본값이 적용될 일이 없습니다. 월 단위를 쓰는 화면이 생기면 그때 `'month'` 로 부르겠습니다. 이유는 주석에 적어 뒀습니다.

---

## 11. `GET /logs` 확정본 반영 — 그리고 인증에서 하나 나왔습니다

### 붙였습니다

목을 평평한 배열로 바꿨고 파라미터 넷을 다 받습니다.

```
getLogs({ from, to, categoryId, limit })
  → { logs: [{ logId, date, categoryId, categoryName, title, usedTimer }], total, hasMore }
```

날짜별 묶기는 말씀대로 화면 쪽에 뒀습니다(`groupLogsByDay`). **서버가 준 순서를 그대로 따릅니다** — 여기서 다시 정렬하면 정렬 규칙이 두 군데가 되고, 한쪽만 바뀌는 날 조용히 어긋납니다. 10장의 `formatDay` 와 붙여서 확인했습니다.

```
평평한 4건 → 8월 18일(1건) · 8월 17일(2건) · 8월 15일(1건)
```

`period` 는 **호출부에서 적어 보내도록** 바꿨습니다. 서버 기본값(month)과 저희 기본값(week)이 다른 채로 두면 읽는 사람이 매번 어느 쪽인지 확인해야 해서, H01 이 `getLogSummary('week')` 로 명시합니다. 이제 어느 쪽 기본값도 실제로 쓰이지 않습니다.

**다만 `GET /logs` · `GET /logs/summary` 는 아직 404 입니다**(방금 실측). 명세가 확정된 것과 배포된 것은 다른 일이라, 응답 형태만 맞춰 두고 스위치는 안 켰습니다. 올라가면 두 줄만 켜면 됩니다.

### ⚠️ 인증 — 서버가 **비밀번호**를 받습니다. 명세서에는 없습니다

실서버 34개 목록을 보고 가입·로그인을 켜려다 발견했습니다.

```
POST /auth/signup {}  → 400 "이메일 · 비밀번호 · 닉네임을 확인해 주세요"
POST /auth/login  {}  → 400 "이메일 형식이 올바르지 않습니다"
```

오류 코드에 `INVALID_CREDENTIALS`(401 이메일·비밀번호 불일치)가 있는 것도 같은 이야기입니다. 그런데 **명세서의 인증에는 비밀번호가 없습니다** — 「게스트 → 이메일+닉네임 가입 → 이메일만으로 로그인」입니다. 저희 `api/auth.js` 가 그 전제로 짜여 있었습니다.

요청 본문은 서버에 맞춰 뒀지만 **가입·로그인 두 줄은 켜지 않았습니다.** 성공 응답을 못 봤고(계정을 만들어야 해서), 비밀번호가 들어오면 화면 흐름도 같이 정해야 합니다 — 시안 B02 「비밀번호 설정」이 명세서에 없는 화면이라 빼려던 참이었는데, 서버가 비밀번호를 받는다면 오히려 필요한 화면이 됩니다.

**명세서와 서버 중 어느 쪽이 맞습니까?** 정해 주시면 그날 바로 켜겠습니다.

`POST /auth/logout` 은 계정 없이 확인할 수 있어서 켰습니다(`{ loggedOut: true }`). `PATCH /me` 도 켰습니다 — 부르는 화면이 아직 없어서 켜 두어도 깨질 것이 없습니다.

### 스위치는 32 / 37 입니다

말씀하신 34개와 **둘 차이인데 이유가 있습니다.**

- **가입 · 로그인** — 위 비밀번호 건이 정리되면 켭니다 (서버엔 살아 있습니다)
- 나머지 셋(`/home` · `/logs` · `/logs/summary`)은 아직 404 입니다

---

확인해 주셔서 감사합니다. 3장 두 건과 11장 인증 건만 봐주시면 나머지는 이대로 두겠습니다.
