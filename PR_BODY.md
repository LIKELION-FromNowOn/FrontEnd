# feat: 실서버 32건 연동 — 관리 항목·오늘의 행동·덜어내기·관리 맥락·기록 탭

> base: `feature/new-screens-0819` ← head: `feature/live-items-today-0820`
>
> 오늘(8/20) 백엔드 배포분을 붙인 것만 담았습니다. 앞 브랜치 27개 커밋과 겹치지 않게
> base 를 `new-screens-0819` 로 뒀습니다. 한꺼번에 `main` 으로 올리시려면 base 만 바꾸면 됩니다.

## 요약

백엔드 배포분 8건에서 시작했는데, 실측해 보니 **켤 수 있는 것이 더 있었고 목과 어긋난 곳도 여러 군데였습니다.** 확인한 것만 켰습니다 — **32 / 37**.

게스트 발급 → 관리 항목 저장 → 상태 체크 → 덜어내기 판정 → 되돌리기 → 오늘의 행동 → 타이머 → 완료까지 **실서버로 끝까지 돕니다.** 브라우저에서 화면으로 확인했습니다.

## 실측으로 드러난 어긋남

목만 보고는 알 수 없고 **실연동해야 드러나는 것들**이었습니다.

| 무엇 | 증상 |
|---|---|
| `PUT /me/items` 가 `itemId` 를 받음 | 항목명을 보내고 있어서 저장이 400 이었습니다 |
| 판정 응답의 목록 키가 `results` | `items` 로 읽어서 **서버가 붙어도 계속 목이 보였습니다** |
| `POST /subtract/evaluate` 에 `checkinId` 필수 | 빼고 불러서 400. `GET /checkins/latest` 로 찾아 붙입니다 |
| 되돌리기에 `evaluationId` 누락 | 화면에서 안 실어 보내고 있었습니다 |
| 거절 사유가 `time`·`fit`·`none` 셋뿐 | 시안 5종(`no_time` 등)은 400 이었습니다 |
| 마스터 3종이 배열 + 식별자가 `id` | `categoryId`·`itemId`·`signalId` 로 읽어 시안 값으로 떨어졌습니다 |
| `POST /state/transition` 에 `checkinId` 필수 | 유예도 일수가 아니라 **시각**(`nextProposalBlockedUntil`)으로 옵니다 |
| 서버가 가입·로그인에 **비밀번호**를 받음 | 명세서엔 없습니다. 확인 요청 중이라 이 두 건만 안 켰습니다 |

## 조용히 틀리던 자리

오류가 안 나서 더 위험했던 것들입니다.

- **타이머 화면이 응답을 못 받으면 목 행동을 그렸습니다.** 4분짜리 화면을 띄워놓고 서버에 없는 id 로 시작하는 꼴이라 없앴습니다. 이제 「덜어내기 먼저」·「오늘 드릴 케어가 없어요」·오류를 갈라서 띄웁니다
- **케어 코치가 「안내문에 없습니다」를 받으면 화면이 죽었습니다.** 그 응답에는 `level` 이 없고 `basis` 도 `{type:"none"}` 뿐인데 표에서 배지를 찾다가 터집니다. 안내문에 없는 질문은 흔합니다
- **완료를 두 번 누르면 기록이 두 줄 남습니다.** 서버가 막지 않아서(`ALREADY_COMPLETED` 는 reroll 에만 걸립니다) `status: done` 이면 타이머를 안 띄웁니다
- **`PUT /me/care` 에 `cautions[].keywords` 를 흘리면** 케어 코치와 예정 충돌이 오류 없이 멈춥니다. `toCareSaveShape` 를 거치게 하고, 빠지면 개발 모드에서 콘솔로 경고합니다

## 기록 탭

- **「이어간 날(연속 달성일)」과 달성률을 뺐습니다.** 명세서의 「하지 않는 것」이고 8/20 에 그렇게 정해졌습니다. 왜 뺐는지 이유까지 코드에 적어 뒀습니다
- H03 을 `GET /subtract/history` 로 붙였습니다. 지난 판정은 `evaluationId` 로 **조회만** 합니다 — 지난 기록을 보려다 오늘 판정이 새로 생기면 안 됩니다
- `GET /logs` 확정본(평평한 배열)에 맞췄습니다. 날짜 묶기와 「8월 18일」 문구는 화면 몫이라 `groupLogsByDay` · `utils/date.js` 로 뒀습니다

## 스위치를 안 켠 5건

`live.js` 는 전역이 아니라 **엔드포인트별**이고, 안 켠 줄에는 이유를 적어 뒀습니다.

| 안 켠 것 | 이유 |
|---|---|
| 회원가입 · 로그인 | 서버가 비밀번호를 받는데 명세서엔 없습니다. 화면 흐름(B02)도 같이 정해야 합니다 |
| `GET /home` · `GET /logs` · `GET /logs/summary` | 아직 404 입니다 |

## 그 밖에

- dev 프록시에서 **허용된 출처인 척 Origin 을 바꿔 보내던 줄**을 지웠습니다. 백엔드가 5174 를 CORS 에 넣어 줬습니다
- `vite preview` 포트를 5173 으로 고정했습니다. 기본값 4173 은 CORS 허용 목록에 없어 403 이라 배포본을 로컬에서 확인할 수 없었습니다. 이제 **개발 프록시로는 확인 못 하는 실제 CORS 경로**를 매번 거칩니다
- 쓰는 곳이 없어진 묵은 목 3개를 지웠습니다. 남겨 두면 고쳐도 화면이 안 바뀝니다

## 확인 부탁

백엔드에 보낼 회신을 `REPLY-BATCH3-0820.md` 에 정리했습니다. 되물어야 할 것이 남아 있습니다.

- 가입·로그인 **비밀번호** — 명세서와 서버 중 어느 쪽이 맞는지
- `topState` · `topSubtracted` 생김새 (`GET /logs/summary` 가 404 라 실측 못 함)
- 「실행 정도」(추천대로 했어요 등)를 저장하는 API 가 없음 — G02 「기록 저장하기」가 아무 데도 안 보냅니다
- `cautions[].keywords` 를 화면이 어디서 받아야 하는지 (서버가 문장에서 안 뽑습니다)

## 확인 방법

```bash
npm run dev            # 5174, 프록시 경유
npm run build && npm run preview   # 5173, 배포본 + 실제 CORS 경로
```

린트·빌드 통과했고, 일곱 화면을 브라우저로 돌려 콘솔 오류가 없는 것까지 확인했습니다.
