# feat: 8/19 시안 3화면 + 남은 stub 3화면 구현 (전 화면 진입 가능)

> base: `develop` ← head: `feature/new-screens-0819`

## 요약

8/19 시안(멋사_해커톤 11) 9장을 반영하고, 시안 없이 남아 있던 화면 3개를 API 명세 기준으로 채웠습니다.
**registry 26화면이 전부 StubScreen 없이 진입 가능합니다** (요구사항 No.5 조건).

## 새 시안 반영

| 화면 | 경로 | 시안 |
|---|---|---|
| 오늘의 컨디션 (허브) | `/condition` | F_WeeklyCondition |
| 내 첫 발자국 관리하기 | `/first-step/manage` | F_FirstStepManage |
| 캐릭터 소개 | `/character` | F_CharacterIntro |

- 홈 히어로 바로가기 2개를 위 화면으로 연결
- 홈이 시안 두 상태를 따릅니다 — 진행 중 루틴이 없으면 「이어가는 첫 발자국」을 빼고 바로가기 문구가 두 줄로 늘어납니다
- 첫 발자국 상세(F03)의 빈 블록 3개를 「그때 상황 · 첫 발자국 · 그다음에 한 일」로 채웠습니다 (NOW-STEP-001의 필드 수와 정확히 일치)

## 시안 없이 API 명세로 구현

| 화면 | 근거 명세 | 지킨 것 |
|---|---|---|
| 기록 | NOW-LOG-001·002 | 달성률·연속일·미완료를 만들지 않음 |
| 케어 시작 타이머 | NOW-TODAY-003·004·005 | `durationSec`은 서버값, 만료 전 완료 허용 |
| 케어 코치 | NOW-COACH-001·002·003 | 답변에 근거(basis)와 원문 문장 번호를 항상 동반 |
| 거절 사유 시트 | NOW-TODAY-008 | 실패로 저장하지 않음을 문구로 명시 |

기존 색·간격 토큰만 써서 짰습니다. 시안이 나오면 값만 갈아끼우면 됩니다.

## 기타

- 「이어가는 첫 발자국」 카드를 `StreakCard`로 분리 (홈·관리 화면 공용)
- 달팽이 에셋 고해상도 교체 + 잎 장식 2종 추가
- `CONDITION_LEVELS` 분리 — 「잘 모르겠어요」는 정도가 아니라 답을 미룬 것이라 그래프 축에 세우지 않습니다

## 확인 부탁드립니다 — 시안↔명세 어긋남 3건

1. **「잘 모르겠어요」가 시안에 아직 없습니다.** 요구사항 No.34로 요청드린 수정 2건 중 하나입니다(기한 8/15). 명세 NOW-STATE-001에서 필수라 앱은 「그냥 그래요」 자리에 넣어 두었습니다.
2. **이상 징후가 명세의 2배입니다.** 명세 14건·5분류 → 시안 약 27개·4분류(관계·생활 합침). 가중치 합산 임계값(5)에 직접 영향이 갑니다.
3. **관리 항목이 피부 카테고리만 있습니다.** 명세 32건·7분류 → 시안 21개·4분류이고 전부 「피부·홈케어」 안쪽입니다. 수면·운동·식사·영양·마음·일상 루틴·건강 관리 6분류가 시안에 없습니다.

2·3번은 마스터 API가 붙으면 서버 값으로 교체될 자리라 지금은 시안 문구를 그대로 두었습니다.

lint·build 통과.
