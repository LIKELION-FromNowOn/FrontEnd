# 기록 탭 — 붙일 API가 없습니다 · 2026-08-20

**보내는 사람** 황인서 (프론트)
**받는 사람** 송원석
**계기** 8/19 기록 탭 시안(H01·H02·H03) 구현 중 발견

> **기능 동결까지 오늘 하루입니다.** 그래서 「추가해 주십시오」가 아니라
> **「추가할지 목으로 갈지 정해 주십시오」**로 올립니다. 어느 쪽이든 프론트는 맞춥니다.

---

## 0. 세 줄

1. **기록 탭 3화면은 다 만들었고 화면은 돕니다.** 목으로 돌고 있습니다
2. **그런데 붙일 API가 없습니다.** 명세서 36건 어디에도 이 화면들이 쓰는 데이터가 없습니다
3. **화면이 쓰는 값을 아래에 JSON으로 적어 뒀습니다.** 추가하기로 하시면 그대로 쓰시면 됩니다

---

## 1. 무엇이 없는가

### ① 기록 홈(H01) — 「이번주 요약」에 들어갈 값이 없습니다

**가장 가까운 API** `GET /logs/summary` (NOW-LOG-002)

```json
{ "total": 4, "byCategory": [ { "categoryName": "피부·홈케어", "count": 2 } ] }
```

**완료 건수와 카테고리 분포**뿐입니다. 시안은 아래 셋을 요구합니다.

| 시안에 있는 것 | 지금 받을 수 있는가 |
|---|---|
| 기록한 날 · 덜어낸 날 · 첫 발자국 이어간 날 (**일수 3종**) | ❌ |
| 이번 주 최빈 컨디션 + 그 일수 + 그 주에 많이 고른 신호 | ❌ |
| 이번 주에 자주 덜어낸 항목 + 횟수 | ❌ |

> **건수(count)와 일수(days)는 다릅니다.** 하루에 두 개를 했으면 건수는 2, 일수는 1입니다.
> 시안의 「기록한 날 5일」은 일수라서 `total` 로는 만들 수 없습니다.

### ② 최근 덜어내기 기록(H03) — **데이터 자체가 없습니다**

**가장 가까운 API** `GET /logs` (NOW-LOG-001) → **완료한 「행동」** 목록입니다.

이 화면이 다루는 것은 **덜어내기 기록**입니다. 다른 데이터입니다.

| | `GET /logs` | H03 이 쓰는 것 |
|---|---|---|
| 무엇 | 오늘의 행동을 완료한 기록 | 덜어내기를 실제로 어떻게 했는지 |
| 필드 | `logId` · `title` · `categoryName` | 날짜 · 항목명 · **실행 정도** · **피부 느낌** · **한 줄 기록** |

**덜어내기 기록을 저장하는 API 도, 조회하는 API 도 명세서 36건에 없습니다.**

이미 만들어 둔 화면(H03-1 「덜어내기 기록」)이 **피부 느낌 5단계 · 실행 정도 3종 · 한 줄 기록(120자)**을 받고 있는데, **받아서 보낼 곳이 없습니다.**

---

## 2. 왜 지금까지 안 드러났는가

**기록 탭 시안이 어제(8/19) 왔습니다.** 그전에는 이 화면들의 모양을 몰라서, 저는 명세서만 보고 「완료 건수와 분포를 보여주는 화면」으로 만들어 뒀었습니다.

어제 시안을 받고 다시 만들면서 **명세서가 아직 이 화면을 따라오지 못했다**는 것이 드러났습니다. 누구의 실수라기보다 **시안이 명세서보다 나중에 온 순서 문제**로 보입니다.

---

## 3. 추가하기로 하시면 — 이대로 쓰시면 됩니다

**화면이 실제로 읽는 필드만 적었습니다.** 이름은 편하신 대로 바꾸셔도 되고, 알려만 주시면 맞추겠습니다.

### ① 주간 요약 — `GET /records/weekly` (또는 `/logs/summary` 확장)

```json
{
  "ok": true,
  "data": {
    "range": { "from": "2026-08-11", "to": "2026-08-17" },
    "recordedDays": 5,
    "reducedDays": 2,
    "streakDays": 5,
    "topCondition": {
      "state": "low",
      "label": "조금 예민",
      "days": 4,
      "totalDays": 7,
      "signals": ["조금 예민", "따가움", "잠 부족"]
    },
    "topReduced": [
      { "name": "레티놀", "count": 4 },
      { "name": "미스트", "count": 1 }
    ]
  },
  "error": null
}
```

| 필드 | 쓰이는 곳 |
|---|---|
| `recordedDays` · `reducedDays` · `streakDays` | 히어로의 카드 3개 |
| `topCondition` | 「이번 주 컨디션」 카드 |
| `topReduced` | 「이번 주 덜어낸 것」 막대 |

> `topReduced` 는 **판정에서 `reduce`·`skip` 이 난 항목을 세면** 될 것 같은데, 정의는 서버 쪽이 맞다고 봅니다.
> 비어 있어도 화면은 안 깨집니다.

### ② 덜어내기 기록 저장 — `POST /subtract/records`

```json
// 요청
{ "evaluationId": "ev_01H8X", "feeling": "sensitive", "did": "as_recommended", "note": "" }
```

| 필드 | 값 |
|---|---|
| `feeling` | `good` · `fine` · `normal` · `sensitive` · `bad` (5단계) |
| `did` | `as_recommended`(추천대로 했어요) · `adjusted`(조금 바꿨어요) · `barely`(거의 못 했어요) |
| `note` | 최대 120자. 자유 입력이라 **위기 신호 사전 필터가 필요합니다** |

> ⚠️ `note` 가 자유 입력이라 `POST /safety/check` 를 거쳐야 하는 **여섯 번째 경로**가 됩니다.
> 지금 5경로로 정리된 목록(안건 2)에 이게 빠져 있습니다.

### ③ 덜어내기 기록 조회 — `GET /subtract/records`

```json
{
  "ok": true,
  "data": {
    "records": [
      {
        "recordId": "sr_003",
        "date": "2026-08-18",
        "title": "레티놀 덜어내기",
        "feeling": "sensitive",
        "did": "as_recommended",
        "note": ""
      }
    ]
  },
  "error": null
}
```

기간 필터는 `?period=all|week|month` 로 쓰고 있습니다.

---

## 4. 세 가지 선택지 — 저는 3번을 권합니다

| # | 무엇 | 백엔드 부담 | 위험 |
|---|---|---|---|
| 1 | **3건 다 추가** | 큼 (동결 당일) | 급하게 넣다 다른 API 를 건드릴 수 있음 |
| 2 | **저장(②)만 추가** | 중간 | 조회가 없어 H03 은 어차피 목 |
| 3 | **기록 탭은 목으로 발표** | 없음 | 기록 탭만 실데이터가 아님 |

### 3번을 권하는 이유

- **기록 탭은 데모에서 「쌓인 것을 보여주는」 화면**입니다. 발표 당일 계정에는 어차피 데이터가 며칠치 없습니다. 실연동이든 목이든 **화면에 보이는 것이 같습니다**
- 반면 **오늘 남은 시간은 마스터 시드와 판정 API**에 쓰는 편이 낫습니다. 그쪽은 목으로 대체하면 온보딩·덜어내기가 통째로 가짜가 됩니다
- 3번을 골라도 **위 JSON 은 그대로 유효합니다.** 동결 이후에 붙이시면 프론트는 주석 처리해 둔 자리에 `useApi` 한 줄만 넣으면 됩니다

> **1번을 고르셔도 프론트는 오늘 안에 붙일 수 있습니다.** 화면·목·파싱이 다 준비돼 있어서
> 스위치와 필드 이름만 맞추면 됩니다. **결정만 주시면 됩니다.**

---

## 5. 지금 상태

**기록 탭 3화면은 완성돼 있고 목으로 돕니다.** 발표에 지장 없습니다.

| 화면 | 상태 |
|---|---|
| H01 기록 홈 | 완성 · 목 |
| H02 이번 주 컨디션 | 완성 · 목 (컨디션 그래프는 오늘의 컨디션 화면과 공용) |
| H03 최근 덜어내기 기록 | 완성 · 목 |
| H03-1 기록 수정 시트 | 완성 · **보낼 곳 없음** |

코드에는 **붙일 API 가 없다는 사실과 필요한 필드를 주석으로 남겨 뒀습니다.** 부르지도 않는 API 를 호출만 해 두면 「연동됐다」는 착시가 생겨서, 통합 디버깅 때 값이 왜 안 바뀌는지 찾느라 시간이 갑니다.

---

## 6. 참고 — 다른 화면은 배선이 끝났습니다

| 화면 | 연결 | 상태 |
|---|---|---|
| 로그인 게스트 시작 | `POST /auth/guest` | **실연동** |
| 홈 | `GET /home` | 목 (스위치 대기) |
| 오늘 컨디션 | 징후 마스터 + `POST /checkins` | 목 |
| 관리 항목 | 마스터 + `PUT /me/items` | 목 |
| 케어 시작 | `GET /today` + 시작 + 완료 | 목 |
| 케어 코치 | `POST /coach/ask` | 목 |
| 덜어내기 결과 | 판정 조회 + 되돌리기 | 목 |
| 첫 발자국 목록 | `GET /footsteps` | 목 |

**「이 묶음 됐습니다」만 올려 주시면 그 묶음 스위치만 켭니다.** 화면 코드는 안 건드립니다.
