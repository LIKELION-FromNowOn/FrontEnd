# 프론트 회신 — 2026-08-19

**보내는 사람** 황인서 (프론트)
**받는 사람** 송원석 (PM) · 일부 항목은 김지현 님
**대상 문서** `RESOLUTION-PLAN-0818.md` · 8/18 카톡 3건

> ⚠️ **「A. 오늘 답이 필요한 것」 3건만 먼저 봐 주십시오.** 나머지는 확인용입니다.
> 이 셋이 막히면 오늘 안에 API를 한 건도 못 붙입니다.

---

## A. 오늘 답이 필요한 것 — 3건

### A-1. 배포 주소와 CORS ★ (C-2 회신)

<!-- ⬇️ 둘 중 하나를 골라서 보내세요. 나머지는 지우십시오. -->

**(배포를 했으면)**

> 배포 주소는 `https://____.netlify.app` 입니다.
> `CORS_ORIGINS` 에 **두 개** 넣어 주십시오.
>
> - `https://____.netlify.app`
> - `http://localhost:5174`
>
> 로컬은 개발 중에 실서버로 붙여 봐야 해서 필요합니다. 이게 없으면 배포 전까지 아무것도 확인을 못 합니다.

**(아직이면)**

> 아직 미정입니다. 말씀대로 임시로 열어 두시고, 주소 나오면 바로 알리겠습니다.
> 다만 **`http://localhost:5174` 만이라도 먼저 넣어 주십시오.** 그러면 배포 주소가 늦어도 오늘 중에 `POST /auth/guest` 하나는 붙여 볼 수 있습니다.

**왜 로컬까지 필요한가** — 프론트에서 실연동을 켜는 스위치를 엔드포인트별로 만들어 뒀습니다. 서버에 올라온 것부터 한 줄씩 켜면서 확인하려면 로컬에서 실서버를 불러야 합니다.

---

### A-2. 실패 응답 JSON 실물 1건 ★

> 401 `UNAUTHORIZED` 아무거나 `curl` 결과를 **그대로** 한 번만 붙여 주십시오.
>
> 성공 응답은 명세서 28건에 `{"ok": true, "data": {...}}` 로 똑같이 적혀 있는데,
> **실패 응답의 바디 모양이 명세서 어디에도 없습니다.** 표에 HTTP 상태·code·message 만 있습니다.
>
> 지금은 아래 세 가지를 모두 받도록 방어해 뒀는데, 실물을 보면 하나로 줄이겠습니다.
>
> ```
> { "ok": false, "error": { "code": "...", "message": "..." } }
> { "code": "...", "message": "..." }
> { "message": "..." }
> ```

**왜 급한가** — 에러 처리 공통 레이어가 여기에 걸려 있습니다. 401 재시도·409 분기(`NO_EVALUATION`, `TIMER_ALREADY_RUNNING`)를 전부 이 모양 위에 얹습니다.

---

### A-3. 인증 — 시안은 있는데 API 명세서에 대응 엔드포인트가 없습니다 ★

> 확인 부탁드립니다. **인증 화면은 시안이 다 나와 있는데, API 명세서에 그 흐름을 받을 엔드포인트가 없습니다.**
>
> **시안에 있는 화면** — A01 로그인(구글 / 이메일) · B01 이메일 입력 · B02 비밀번호 설정 · B03 이메일 인증(인증코드 6자리 · 재발송 · 회원가입)
>
> **명세서에 있는 인증 API** — 셋뿐입니다.
>
> | 명세서 | 하는 일 |
> |---|---|
> | `POST /auth/guest` | 게스트 토큰 발급 |
> | `POST /auth/signup` | 이메일 + 닉네임으로 계정 생성 |
> | `POST /auth/login` | **이메일만** 받아 토큰 발급 |
>
> 비밀번호를 받는 곳도, 인증코드를 보내거나 확인하는 곳도, OAuth 시작점도 없습니다.
> `NOW-AUTH-003` 비고에 **「인증 방식 미확정 — JWT · 세션 · 매직링크 중 결정 필요」**로 남아 있는데,
> 8/14에 확정된 것은 **토큰 방식(JWT)**이지 **가입·로그인 절차**는 아닌 것으로 보입니다.
>
> **화면은 시안대로 다 만들어 두었습니다.** 인증코드 6칸·재발송까지 동작합니다.
> 지금 필요한 것은 **B01~B03이 부를 API 3~4건**입니다.
>
> - 이메일 중복/가입 여부 확인
> - 비밀번호 설정(또는 비밀번호를 안 쓴다면 인증코드 발송)
> - 인증코드 확인
> - 인증코드 재발송
>
> **어느 쪽으로 갈지만 정해 주시면 프론트는 맞춥니다.**
>
> 1. **엔드포인트를 추가한다** — 시안 그대로 갑니다. 백엔드에 위 3~4건이 필요합니다
> 2. **명세서대로 줄인다** — B02(비밀번호)를 빼고 이메일 인증만 남기거나, B01~B03을 통째로 빼고 게스트 + 이메일 가입으로 갑니다. 이 경우 **시안이 사라지는 것이라 지현님 확인이 필요합니다**
>
> **동결 전에 정해야 합니다.** 8/19 10:00 회의 안건 7개에 이 건이 없어서 따로 올립니다.

**프론트 메모** — 어느 쪽이든 화면은 이미 있으니 프론트 작업은 크지 않습니다(연결 30분 / 삭제 30분). **판단 기준은 백엔드에 3~4건을 더 얹을 시간이 있느냐**입니다. 없으면 2번인데, 그때는 시안이 버려지는 것이라 지현님께 먼저 말씀드리는 게 맞습니다. 구글 로그인은 명세서에도 없고 백엔드 부담이 제일 크니, 시간이 빠듯하면 **이것부터 빼는 것**을 제안드립니다.

---

## B. 카톡 3건 회신

### B-1. `excludedBy` — 말씀대로 `floor` 로 되어 있었습니다 (C-1 회신)

> 확인했습니다. **저희 목 데이터에 `floor` 로 들어가 있었습니다.** `medical` 로 고치고, 확정값 두 개(`medical` · `clinicNote`)만 쓰도록 상수로 고정했습니다.
>
> 참고로 `floor` 는 **예전에 원석님이 카톡으로 알려주신 값**이라 그대로 들어가 있었던 것입니다. 저희가 임의로 넣은 게 아닙니다.
>
> **문장 매핑은 그대로 유효한 것으로 이해했습니다.** 이름만 바뀌었지 사유 두 개는 같은 것으로 보입니다.
>
> | `excludedBy` | 서버가 주는 `reason` |
> |---|---|
> | `medical` (구 `floor`) | 이 항목은 앱이 판단하지 않습니다. |
> | `clinicNote` | 클리닉에서 받으신 안내가 우선입니다. 앱이 바꾸지 않습니다. |
>
> `reason` 은 화면에서 만들지 않고 서버가 준 문장을 그대로 출력합니다. 하드코딩 없습니다.
>
> **하나만 더 확인 부탁드립니다.**
> `floor` 는 원래 하한선(생리적 필수 / 권장 / 선택 / 판정 제외) 개념이었는데 이름이 `medical` 로 바뀐 것이라면,
> **의료 항목이 아닌 일반 항목에도 `medical` 이 붙을 수 있습니까?**
>
> 명세서 `NOW-SUB-001` 처리 순서에 **①판정 제외**와 **⑥하한선 보정**이 별개 단계로 있어서, 둘이 `medical` 하나로 합쳐진 것인지가 헷갈립니다.
>
> 저는 「판정 제외 = 의료·클리닉 항목」으로 보고 목 데이터를 마스크팩 → 처방약으로 바꿔 뒀습니다.
> 하한선 때문에 마스크팩 같은 일반 항목도 제외될 수 있으면 되돌리겠습니다.

---

### B-2. 글자 수 상한 — 김지현 님께 (C-3 관련)

> 지현님, 원석님이 물으신 글자 수 상한 건입니다. 프론트에서도 입력을 막아야 해서 같이 봅니다.
> 닉네임 · 클리닉 이름 · 첫 발자국 익명 프로필 · 관리 항목 직접 입력 네 곳입니다.
> **시안에 명시가 없으면 「100자 그대로」라고만 답해 주셔도 됩니다.** 그러면 프론트도 100자로 맞추겠습니다.

---

## C. 시안 ↔ 명세서 어긋남 — 3건 (김지현 님 · 원석님 함께)

8/14에 두 건 찾아 드렸던 것과 같은 종류입니다. **셋 다 마스터 데이터와 판정에 걸립니다.**

### C-1. 「잘 모르겠어요」가 아직 시안에 없습니다 ★

**요구사항 No.34 로 요청드린 수정 2건 중 하나**입니다(기한 8/15).

| | 값 |
|---|---|
| 명세서 `NOW-STATE-001` | 5단계이고 **「모르겠다」가 필수**. 상태값 `unknown` 허용 |
| 8/19 시안 (H02-1 · F_WeeklyCondition_EditCondition) | 아주 좋아요 / 괜찮아요 / **그냥 그래요** / 좀 쳐져요 / 많이 지쳤어요 — 전부 「정도」 |

**앱은 명세서를 따라** 「그냥 그래요」 자리에 **「오늘은 잘 모르겠어요 🤔」**를 넣어 두었습니다. (문구는 원석님이 예전에 선호하신 것 그대로입니다.)

> **다만 이번 시안은 안에서 일관됩니다.** H02 그래프 세로축도 5단계로 그려져 있어서, **의도적으로 5단계(모르겠다 없이)로 가시는 것인지** 확인이 필요합니다.
>
> - **명세서대로 간다** → 시안에 「잘 모르겠어요」를 넣고 그래프 축은 4단계로 (모르겠다는 정도가 아니라 답을 미룬 것이라 축에 세울 수 없습니다)
> - **시안대로 간다** → 명세서 `NOW-STATE-001` 에서 `unknown` 을 빼야 하고, 컨디션 미선택을 어떻게 할지 다시 정해야 합니다
>
> **현재 앱은 명세서 쪽으로 구현돼 있습니다.** 시안대로 가기로 하면 알려 주십시오.

### C-2. 이상 징후가 명세서의 2배입니다

| | 값 |
|---|---|
| 명세서 `NOW-MASTER-003` | **14건** · 5분류(피부 · 수면 · 마음 · 관계 · 생활) · 가중치 있음 |
| 시안 | **약 27개** · 4분류(관계·생활을 하나로 합침) |

**신호 강도는 가중치 합산이고 전환 임계값이 5입니다.** 14개 기준으로 잡은 가중치에 27개를 넣으면 임계값이 안 맞습니다. 마스터 검수 때 어느 쪽으로 정하실지 알려 주십시오.

### C-3. 관리 항목이 피부 카테고리 안쪽만 있습니다

| | 값 |
|---|---|
| 명세서 `NOW-MASTER-002` | **32건** · **7분류**(피부·홈케어 / 수면 / 운동 / 식사·영양 / 마음 / 일상 루틴 / 건강 관리) |
| 시안 | **21건** · 4분류(세안·클렌징 / 보습·진정 / 자외선 차단 / 집중 케어) — **전부 「피부·홈케어」 안쪽 세분화** |

수면 · 운동 · 식사·영양 · 마음 · 일상 루틴 · 건강 관리 **6분류가 시안에 없습니다.**

> 오늘의 행동이 「사용자가 고른 카테고리 안에서만」 생성되므로(`NOW-TODAY-001` 생성 규칙 1),
> 피부 항목만 고를 수 있으면 **수면·마음 카테고리의 행동은 영원히 안 나옵니다.**
> 첫 발자국 사례에는 수면·마음이 있어서 화면끼리 어긋나 보입니다.

**C-2 · C-3 은 마스터 API 가 붙으면 서버 값이 정답이 됩니다.** 지금은 시안 문구를 그대로 두었고, 붙는 즉시 서버 값으로 바뀝니다. 검수(요구사항 No.16) 때 어느 쪽으로 닫으실지만 알려 주십시오.

---

## D. 확인만 부탁드리는 것 — 급하지 않음

| # | 무엇 | 내용 |
|---|---|---|
| D-1 | `footstepCard` 를 누가 고르나 | **두 문서가 반대로 적혀 있습니다.** 홈 집계(`NOW-HOME-001`)는 「서버가 고릅니다」, 첫 발자국(`NOW-STEP-001`)은 「클라이언트가 고릅니다」. 지금은 홈 응답에 `footstepCard` 가 들어오는 것으로 보고 그대로 씁니다 |
| D-2 | 주간·월간 발견 화면 | 홈 응답 `unlock` 에 `weeklyOpen` · `monthlyOpen` · `monthlyNeed` 가 있는데 **기능 명세서 59건에 해당 화면이 없습니다.** 만드는 화면입니까? |
| D-3 | 온보딩 1회 노출 플래그 | `NOW-STEP-004` 「다시 띄우지 않는다」인데 `GET /me` 응답에 그 값이 없습니다(`hasCheckin` · `itemCount` 만 있음). 서버에 안 두시면 프론트가 localStorage 로 처리하겠습니다 — **그렇게 가도 되는지만** 알려 주십시오 |
| D-4 | 재제안 유예 기간 | `NOW-STATE-003` 이 「미확정 · 예시는 3일 기준」으로 되어 있습니다. 화면에 기간을 표시할지 말지가 여기 걸립니다 |
| D-5 | `generatedBy` · `expiresAt` | 오늘의 행동에 이 둘이 실제로 내려옵니까? LLM 폴백일 때 화면 표시를 다르게 할지 정하려고 합니다 |

---

## E. 프론트 현황 (참고)

### 화면 — 28개 전부 진입 가능

8/19 시안 두 묶음(멋사_해커톤 11 · 12)을 모두 반영했습니다.

| 묶음 | 화면 |
|---|---|
| 새로 만든 것 | 오늘의 컨디션 허브 · 내 첫 발자국 관리 · 캐릭터 소개 · 기록 홈(H01) · 이번 주 컨디션(H02) · 최근 덜어내기 기록(H03) |
| 시안 없이 API 명세로 만든 것 | 케어 시작 타이머 · 케어 코치 · 거절 사유 시트 |

시안이 없던 화면은 **기존 색·간격 토큰만** 써서 짰습니다. 시안이 나오면 값만 갈아끼우면 됩니다.

### API 연동 준비 — 목으로 전부 돌아갑니다

- **엔드포인트별 실연동 스위치 36건**을 요구사항 전달 묶음대로 만들어 뒀습니다
  (`auth 13` · `master 3` · `subtract 3` · `today 11` · `log 3` · `note 1` · `safety 2`)
- **「이 묶음 됐습니다」를 슬랙에 올려 주시면 그 묶음만 켭니다.** 전역 스위치 하나로 두면 서버에 1건뿐인 지금 켜는 순간 전 화면이 깨져서 이렇게 나눴습니다
- 목 응답을 **명세서 응답 예시와 같은 모양**으로 맞춰 뒀습니다. 스위치만 켜면 화면이 그대로 돕니다
- 인증은 JWT `Authorization: Bearer` 로 맞췄습니다 (8/14 확정 반영). 게스트 토큰은 가입 시 `guestToken` 으로 동봉해 데이터 이전까지 연결해 뒀습니다

### 고친 버그 3건

1. **`excludedBy: 'floor'`** → `medical` (위 B-1)
2. **관리 항목 최소 개수가 화면에 안 걸려 있었습니다.** 1개만 골라도 다음으로 넘어갔습니다. 명세서 `NOW-ITEM-002` 「최소 3개」와 회의 안건 6 기본값에 맞춰 3개로 막았습니다. 회의에서 다르게 정해지면 상수 한 줄만 고치면 됩니다
3. **CSS 색 변수 세 개가 정의되지 않은 채 쓰이고 있었습니다.** 값이 없으면 CSS는 조용히 넘어가서, 기기 프레임과 **B03 인증코드 6칸이 투명하게 렌더링**되고 있었습니다(칸이 아예 안 보였습니다). 전체를 훑어 미정의 변수를 없앴고, 화면 바깥 테두리도 시안처럼 넣었습니다

### 이해한 대로 확인 한 줄

**`POST /safety/check` 는 프론트가 따로 부르지 않는 것으로 이해했습니다.** 해결방안 안건 2의 5경로가 전부 서버 엔드포인트라 백엔드 내부 사전 필터로 보입니다. 프론트는 응답에 `flagged` 가 오면 상담 안내를 띄우기만 하겠습니다. **틀렸으면 알려 주십시오.**

(덧붙여 — 명세서 `NOW-SAFE-001` 주석의 `custom_item | custom_signal | todo | coach` 나열은 원석님 지적대로 낡았습니다. 프론트도 그 주석은 무시하겠습니다.)

---

## 요약 — 오늘 제가 기다리는 것

| 순위 | 무엇 | 없으면 |
|---|---|---|
| 1 | CORS 에 `localhost:5174` 등록 + 배포 URL | **API를 한 건도 못 붙입니다** |
| 2 | 실패 응답 JSON 실물 1건 | 에러 처리 공통 레이어를 못 닫습니다 |
| 3 | 인증 — 엔드포인트를 추가할지 화면을 줄일지 | 동결 후에는 못 고칩니다. 시안이 걸려 있어 지현님도 관련 |
| 4 | 「잘 모르겠어요」 — 명세서 쪽인지 시안 쪽인지 | 컨디션 화면과 그래프를 다시 만들어야 할 수 있습니다 |

1번만 오면 **오늘 안에 `POST /auth/guest` 하나는 붙여 보겠습니다.**
그 한 건으로 CORS · 토큰 · 응답 봉투 · 에러 형태가 한 번에 검증되면, 나머지 35건은 스위치만 켜는 반복 작업입니다.
반대로 이걸 오늘 못 뚫으면 **내일 CORS 하나에 오전이 날아갑니다.**
