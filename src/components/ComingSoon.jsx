import './ComingSoon.css'

/**
 * 「이번 범위에 넣지 않기로 한」 기능 자리.
 *
 * 시안에는 있는데 붙일 API 를 만들기로 한 적이 없는 것들이다
 * (비밀번호 변경 · 연결계정 · 회원탈퇴 · 알림 설정 · 서비스 안내 · 문의하기).
 * 노션 NOW-AUTH-003 마지막 문단에 「이번 범위에 넣지 않는 것」으로 적혀 있다.
 *
 * 줄을 지우지 않고 남겨 두기로 했다(2026-08-21 원석님 결정) —
 * 지우면 시안이 비어 보이고, 남겨 두면 「다음 단계」로 설명이 된다.
 * 대신 **눌렀을 때 아무 일도 안 일어나면 고장 난 것처럼 보이므로** 준비 중임을 알린다.
 */

/** 화면 전체가 준비 중일 때 맨 위에 두는 띠 */
export function ComingSoonBanner({ children = '다음 단계에서 준비 중인 기능이에요' }) {
  return (
    <p className="coming" role="status">
      {children}
    </p>
  )
}
